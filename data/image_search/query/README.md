# Image Search Queries

Place query images here for Experiment 13.

These are the "new" images you want to find the closest match for
in the enrolled library. The tool will compute the perceptual hash
and correlate against all enrolled fingerprints.
